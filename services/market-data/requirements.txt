# Nexxore Market Data Service - Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
aiosqlite>=0.19.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Optional: Redis cache (falls back to in-memory if not available)
# redis>=5.0.0
